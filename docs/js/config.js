// ============================================================
// 应用配置 —— 云端同步
// 说明：
// - 曾尝试用 GitHub token 实现同步，但 GitHub 会自动吊销泄露在
//   公开仓库中的 token，因此该方案不可行。
// - 当前置空：应用走「纯本地演示」模式，零配置、可直接使用。
//   学生扫码后看到课程示例，发帖/留言仅保存在各自设备的浏览器中。
// - 若日后接入 LeanCloud / Firebase 等公开 BaaS 实现真同步，
//   只需在此填入对应凭据即可（那些平台的 key 设计为可公开嵌入）。
// ============================================================
window.APP_CONFIG = {
  GITHUB_TOKEN: ''
}
